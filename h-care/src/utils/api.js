// API configuration and utilities
// ALL API ENDPOINTS REMOVED - USING LOCAL STORAGE ONLY

// Placeholder exports to prevent import errors
export const authAPI = {};
export const productsAPI = {};
export const ordersAPI = {};

export default {
  authAPI,
  productsAPI,
  ordersAPI,
};
